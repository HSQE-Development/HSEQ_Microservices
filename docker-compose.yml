version: '3.8'
services:
  microservices:
    build: 
      context: .
      dockerfile: Dockerfile
    command: sh -c "/usr/local/bin/wait-for-it.sh 162.215.135.37:33080
    restart: always
    container_name: hseq_microservices
    ports:
      - "7001:3000"
    env_file:
      - ./.env
    depends_on: []